<?php

namespace Tests\Unit;

use Tests\TestCase;
use App\Models\Ruangan;

class UpdateRuanganTest extends TestCase
{
    /*
    public function test_bisa_update_ruangan()
    {
        // Create a Ruangan instance
        $ruangan = Ruangan::create([
            'idruangan' => 1, // Set a specific value
            'nama_ruangan' => 'Old Name',
        ]);

        // New name to update
        $newName = 'New Name';

        // Update the nama_ruangan field
        $ruangan->update(['nama_ruangan' => $newName]);

        // Refresh the model instance from the database
        $ruangan->refresh();

        // Assert that the nama_ruangan field has been updated
        $this->assertEquals($newName, $ruangan->nama_ruangan);
    }
    */
}