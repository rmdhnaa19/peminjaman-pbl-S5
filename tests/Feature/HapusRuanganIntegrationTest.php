<?php

namespace Tests\Feature;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Foundation\Testing\WithFaker;
use Tests\TestCase;
use App\Models\Barang;

class HapusRuanganIntegrationTest extends TestCase
{
    /*
    public function test_integrationTest_hapus_barang()
    {
        // Cari barang berdasarkan ID
        $barang = Barang::find(3);

        // Lakukan penghapusan barang
        $barang->delete();

        // Memeriksa apakah barang tidak ada setelah penghapusan
        $this->assertNull(Barang::find(3), 'Barang berhasil dihapus.');
    }
    */
}
